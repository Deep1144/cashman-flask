version: '3.4'

services:
  cashmanflaskproject:
    image: cashmanflaskproject
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    # volumes: 
    #   - ./:/usr/src/app
